/**
 * TraitDebugger Styles
 *
 * @description Styles for TraitDebugger component
 */
import React from 'react';

export const useStyles = () => {
  return {
    container: {
      display: 'flex',
      flexDirection: 'column' as React.CSSProperties['flexDirection'],
      padding: '1rem',
    },
  };
};
