/**
 * BitcoinService Mock
 * 
 * @description Mock data for BitcoinService
 */

/**
 * Mock data for BitcoinService
 */
export const BitcoinServiceMock = {
  // Mock data
};

/**
 * Mock function for BitcoinService
 */
export const BitcoinServiceMockFn = jest.fn().mockImplementation(() => {
  return BitcoinServiceMock;
});
