/**
 * ParticleService Mock
 * 
 * @description Mock data for ParticleService
 */

/**
 * Mock data for ParticleService
 */
export const ParticleServiceMock = {
  // Mock data
};

/**
 * Mock function for ParticleService
 */
export const ParticleServiceMockFn = jest.fn().mockImplementation(() => {
  return ParticleServiceMock;
});
