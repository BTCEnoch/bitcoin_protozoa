/**
 * Particles Mock
 * 
 * @description Mock data for Particles
 */

/**
 * Mock data for Particles
 */
export const ParticlesMock = {
  // Mock data
};

/**
 * Mock function for Particles
 */
export const ParticlesMockFn = jest.fn().mockImplementation(() => {
  return ParticlesMock;
});
