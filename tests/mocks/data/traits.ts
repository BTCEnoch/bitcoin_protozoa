/**
 * Traits Mock
 * 
 * @description Mock data for Traits
 */

/**
 * Mock data for Traits
 */
export const TraitsMock = {
  // Mock data
};

/**
 * Mock function for Traits
 */
export const TraitsMockFn = jest.fn().mockImplementation(() => {
  return TraitsMock;
});
