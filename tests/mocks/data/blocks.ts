/**
 * Blocks Mock
 * 
 * @description Mock data for Blocks
 */

/**
 * Mock data for Blocks
 */
export const BlocksMock = {
  // Mock data
};

/**
 * Mock function for Blocks
 */
export const BlocksMockFn = jest.fn().mockImplementation(() => {
  return BlocksMock;
});
